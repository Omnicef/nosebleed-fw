# CLAUDE.md

All project guidance lives in @AGENTS.md — read it in full before doing anything.

This stub exists only because Claude Code reads `CLAUDE.md` while opencode, Codex and Cursor read `AGENTS.md`. **Do not add content here.** Edit `AGENTS.md` instead, so every tool sees the same instructions.

The build plan is in `PLAN.md`. Do phases in order; Phase 0 is a go/no-go gate.
